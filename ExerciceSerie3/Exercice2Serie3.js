function elementPresent(tableau, element){
    return tableau.includes(element);


    /*Include permet de vérifié si un élément existe dans le tableau, soit true ou false*/
}
console.log(elementPresent ([2, 3, 4],3));
